{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build":     { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "dev":       { "cache": false, "persistent": true },
    "lint":      { "outputs": [] },
    "typecheck": { "outputs": [] },
    "test": {
      "dependsOn": [],
      "outputs": ["coverage/**"],
      "cache": false,
      "// passThroughEnv": "Turbo runs tasks in strict env mode: vars not listed here are stripped from the task process. The suite reads these from the environment when no .env.test file is present (i.e. in CI), so they must pass through or the TEST_DATABASE guard aborts.",
      "passThroughEnv": [
        "TEST_DATABASE",
        "DATABASE_URL",
        "DIRECT_DATABASE_URL",
        "REDIS_URL",
        "BCRYPT_ROUNDS",
        "JWT_PRIVATE_KEY",
        "JWT_PUBLIC_KEY"
      ]
    }
  }
}
