node_modules
dist
.turbo
.env
.env.main
.env.local
.env.*.local
# Every concrete env file is secret and stays local. Only *.env.example is
# committed. .env.test holds throwaway TEST-database creds; .env.production must
# never live in the repo (prod secrets belong in Railway / GitHub Secrets).
.env.test
.env.production
.env.staging
!.env.example
!*.env.example
*.log
.DS_Store
coverage
.next
build
*.tsbuildinfo

# Local database backups — contain user data, never commit to GitHub
backups/*.json
backups/*.json.bak

# Dev logo uploads (local fallback when R2 is not configured)
apps/api/uploads/

# graphify knowledge-graph output (generated locally, regenerable via /graphify)
graphify-out/
